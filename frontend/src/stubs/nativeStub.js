/**
 * nativeStub.js
 * Empty stub returned by metro.config.js when bundling native-only modules for web.
 * The real implementations are guarded by Platform.OS !== 'web' checks in source.
 */
module.exports = {};
